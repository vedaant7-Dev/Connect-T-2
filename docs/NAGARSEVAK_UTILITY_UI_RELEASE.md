# Release Note

The Nagarsevak Home dashboard removes the duplicate Alerts & News card, adds exact utility Start/End Time pickers with automatic duration, and relies on the authenticated backend ward assignment instead of a ward selector.
